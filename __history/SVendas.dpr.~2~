program SVendas;

uses
  Vcl.Forms,
  U_inicial in 'Fontes\U_inicial.pas' {Form1};

{$R *.res}

begin
  Application.Initialize;
  Application.MainFormOnTaskbar := True;
  Application.Title := 'SVendas';
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
